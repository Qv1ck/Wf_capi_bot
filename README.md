# 🤖 Warframe Telegram Bot

Телеграм-бот для игры Warframe с информацией о варфреймах, уведомлениями о циклах и ротациях миссий.

## 📋 Возможности

- **Поиск информации о варфреймах**: способности и места получения деталей
- **Автоматические уведомления**:
  - Равнины Эйдолона (День/Ночь)
  - Фортуна (Тепло/Холод)
  - Камбионский Дрейф (Воум/Фэз)
  - Ротации миссий синдикатов (каждые 2.5ч)
- **Проверка статуса**: текущие фазы всех локаций
- **Работает в личных сообщениях и группах**

## 🚀 Быстрый старт

### 1. Установка Node.js

**Windows:**
1. Скачай Node.js с [nodejs.org](https://nodejs.org/) (версия LTS)
2. Запусти установщик и следуй инструкциям
3. Проверь установку: открой командную строку (Win+R → `cmd`) и выполни:
```bash
node --version
npm --version
```

**macOS:**
```bash
brew install node
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install nodejs npm
```

### 2. Создание бота в Telegram

1. Найди [@BotFather](https://t.me/BotFather) в Telegram
2. Отправь команду `/newbot`
3. Введи имя бота (например, `Warframe Helper`)
4. Введи username бота (например, `warframe_helper_bot`)
5. **Скопируй токен** (выглядит как `7795216377:AAEHw_LHk2rIa9I6ntPM6QYozU4EsXDH6QQ`)

**Важно для работы в группах:**
1. Отправь BotFather: `/mybots`
2. Выбери своего бота
3. Bot Settings → Group Privacy → **Turn off**
4. Это позволит боту реагировать на упоминания в группах

### 3. Скачивание проекта

**Вариант А: Через Git**
```bash
git clone https://github.com/ВАШ_USERNAME/warframe-telegram-bot.git
cd warframe-telegram-bot
```

**Вариант Б: Скачать ZIP**
1. Нажми кнопку **Code** → **Download ZIP**
2. Распакуй архив
3. Открой командную строку в папке проекта

### 4. Настройка токена

Создай файл `.env` в корне проекта:

**Windows (командная строка):**
```bash
echo BOT_TOKEN=ВАШ_ТОКЕН_ОТ_BOTFATHER > .env
```

**macOS/Linux:**
```bash
echo "BOT_TOKEN=ВАШ_ТОКЕН_ОТ_BOTFATHER" > .env
```

**Или создай вручную:**
1. Создай текстовый файл `.env` (точка в начале обязательна!)
2. Впиши в него одну строку:
```
BOT_TOKEN=7795216377:AAEHw_LHk2rIa9I6ntPM6QYozU4EsXDH6QQ
```
(замени на свой токен)

### 5. Установка зависимостей

```bash
npm install
```

### 6. Запуск бота

```bash
node js_bot.js
```

Увидишь:
```
============================================================
🤖 WARFRAME BOT
============================================================
✓ Бот инициализирован
✓ Локальные базы данных загружены
✓ Система циклов активирована
✓ Система ротаций синдикатов активирована
✓ Подписчики: 0
============================================================
✓ 🚀 Бот запущен и готов к работе!
============================================================
```

## 📱 Использование

### Команды

- `/start` - Список команд
- `/status` - Статус всех локаций
- `/status Равнины Эйдолона` - Статус конкретной локации
- `/search Excalibur` - Поиск варфрейма
- `/subscribe` - Подписаться на уведомления
- `/unsubscribe` - Отписаться

### В личных сообщениях

Просто напиши название варфрейма:
```
Nova
Rhino
Wisp
```

### В группах

Упомяни бота:
```
@ваш_бот Excalibur
/search Volt
```

## 🛠️ Структура проекта

```
warframe-telegram-bot/
├── js_bot.js                          # Главный файл бота
├── .env                               # Токен бота (НЕ коммитить!)
├── bot_state.json                     # Состояние (создаётся автоматически)
├── package.json                       # Зависимости
├── package-lock.json                  # Версии пакетов
├── warframe_abilities_ru.json         # Способности варфреймов
├── warframe_drop_locations_ru.json    # Места получения
├── warframe_cycles_ru.json            # Циклы локаций
├── warframe_syndicate_bounties_ru.json # Ротации миссий
├── warframe_name_aliases_ru.json      # Русские названия
├── .gitignore                         # Игнорируемые файлы
└── README.md                          # Эта инструкция
```

## ⚙️ Продвинутая настройка

### Запуск в фоне (Linux/macOS)

**С помощью PM2:**
```bash
npm install -g pm2
pm2 start js_bot.js --name warframe-bot
pm2 save
pm2 startup
```

**Управление:**
```bash
pm2 status          # Статус
pm2 logs warframe-bot  # Логи
pm2 restart warframe-bot  # Перезапуск
pm2 stop warframe-bot     # Остановка
```

### Запуск на сервере (VPS)

1. Подключись к серверу по SSH
2. Установи Node.js (см. выше)
3. Склонируй репозиторий
4. Настрой `.env`
5. Запусти с PM2

### Обновление бота

```bash
git pull origin main
npm install
pm2 restart warframe-bot
```

## 🐛 Решение проблем

### Ошибка "TOKEN не найден"
- Проверь, что файл `.env` создан правильно
- Токен должен быть на одной строке без пробелов
- В Windows файл может называться `.env.txt` - переименуй

### Бот не отвечает в группе
- Отключи Privacy Mode в @BotFather (см. пункт 2)
- Удали бота из группы и добавь заново
- Проверь, что упоминаешь бота через `@`

### "Module not found"
```bash
rm -rf node_modules package-lock.json
npm install
```

### Бот падает
- Проверь логи: `pm2 logs warframe-bot`
- Убедись, что все JSON-файлы на месте
- Проверь интернет-соединение

## 📝 Лицензия

MIT License - используй свободно!

## 🤝 Вклад в проект

1. Форкни репозиторий
2. Создай ветку: `git checkout -b feature/new-feature`
3. Закоммить: `git commit -am 'Add new feature'`
4. Запуш: `git push origin feature/new-feature`
5. Создай Pull Request

## 📧 Контакты

Вопросы и предложения - создавай Issue на GitHub!

---

Made with ❤️ for Warframe community